export class Contact {
  constructor(full_name, email, tel, tags, id) {
    this.full_name = full_name;
    this.email = email;
    this.tel = tel;
    this.tags = tags;
    this.id = id;
  }

  update() {
    // updates contact
  }

  delete() {
    // removes contact
  }
}